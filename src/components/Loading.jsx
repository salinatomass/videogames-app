import { Typography } from '@mui/material';

const Loading = () => {
  return (
    <Typography
      variant="h6"
      sx={{
        color: 'white',
      }}
    >
      Loading...
    </Typography>
  );
};

export default Loading;
